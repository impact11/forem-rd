<%= simple_form_for [:admin, @forum] do |f| %>
  <%= f.input :category, :collection => Forem::Category.all, :include_blank => false %>
  <%= f.input :title %>
  <%= f.input :description, :as => :text %>
  
  <%= f.submit %>
<% end %>
